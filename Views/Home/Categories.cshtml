<h1>Categories</h1>
@if (@Model.Count == 0)
<!-- What is @Model.Count pointing towards here? The Category.cs model? What there? -->
{
  <h3>No categories have been added yet!</h3>
}
@foreach (var category in Model)
{
  <h3><a href='/categories/@category.GetId()'>@category.GetName()</a></h3>
}
<!-- What is the difference between @category and @Model? -->
<p><a href='/'>Back home</a></p>
